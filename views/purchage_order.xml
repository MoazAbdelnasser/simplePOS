<?xml version="1.0" ?>
<odoo>
    <data>

        <!-- Hide un-necessary Fields and Buttons-->
        <record id="purchage_form_view_inherited_moaz" model="ir.ui.view">
            <field name="name">purchase.order.inherited.moaz</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <!-- Now we will hide all un-neccessary Fields from Partners
                Buttons ( -  - )-->
                <xpath expr="//field[@name='state']" position="attributes">
                    <attribute name="statusbar_visible">{draft,purchase,done}</attribute>
                </xpath>
                <button name="action_rfq_send" position="attributes">
                    <attribute name="invisible">1</attribute>
                </button>
                <button name="confirm_reminder_mail" position="attributes">
                    <attribute name="invisible">1</attribute>
                </button>
                <button name="print_quotation" position="attributes">
                    <attribute name="invisible">1</attribute>
                </button>
                
            </field>
        </record>
        <!-- Test header -->

    </data>
</odoo>
